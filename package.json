{
  "name": "tabungan-smart-contract",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npx hardhat run scripts/deploy.js --network fuji"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.13.0"
  },
  "author": "Rahmat Lovers",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
